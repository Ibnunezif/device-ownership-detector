// common js to es6 module
// DB connection and cloudinary config moved to /config folder
//This feature assumes authentication middleware will populate:
// req.user = { _id, role }
// make sure to replace mockStudentAuth with real auth middleware
